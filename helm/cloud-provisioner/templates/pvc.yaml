apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "cloud-provisioner.fullname" . }}-pvc
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: {{ .Values.persistence.storageClass }}
  resources:
    requests:
      storage: {{ .Values.persistence.size }}
